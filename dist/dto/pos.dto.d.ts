export declare class PosDto {
    currency: string;
    posType: string;
    qrToken: string;
    simpleCode: string;
    isActive: boolean;
    storeId: string;
    constructor(currency: string, posType: string, qrToken: string, simpleCode: string, isActive: boolean, storeId: string);
}
